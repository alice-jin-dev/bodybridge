[project]
name = "bodybridge"
version = "0.1.0"
description = "Connect embodied devices to cloud AI via MCP"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.28.1",
    "websockets>=16.1.1",
]
